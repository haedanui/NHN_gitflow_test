public class Main{
    public static void main(String[] args) {
        int x = 10;
        int y = 10;
        Add add = new Add();
        add.add(x, y);
        sub.sub(x, y);
        Mul.mul(x, y);
        div.div(x, y);
    }
}